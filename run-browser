#!/bin/bash
SCRIPT_DIR=$(cd "$(dirname "$0")" ; pwd -P)

REYARN=$1

if [ "$REYARN" == "1" ]; then
    cd $SCRIPT_DIR/kmp && ./gradlew kotlinUpgradeYarnLock
fi
cd $SCRIPT_DIR/kmp && ./gradlew jsBrowserProductionRun
