#!/bin/bash
SCRIPT_DIR=$(cd "$(dirname "$0")" ; pwd -P)

REYARN=$1

if [ "$REYARN" == "1" ]; then
    $SCRIPT_DIR/kmp/gradlew kotlinUpgradeYarnLock
fi
$SCRIPT_DIR/kmp/gradlew jsBrowserDevelopmentExecutableDistribution
